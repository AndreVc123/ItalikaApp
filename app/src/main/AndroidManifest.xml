<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.bluecode.applicationitalika">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="com.dev.bluecode.applicationitalika.ItalikaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApplicationMap"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.dev.bluecode.applicationitalika.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".productsModule.ProductsActivity" android:configChanges="orientation"/>
        <activity android:name=".detailProductModule.DetailProductActivity" android:configChanges="orientation"/>
        <activity android:name=".cartModule.CartActivity" android:configChanges="orientation" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

</manifest>